name: Deploy Next.js App via Compose

on:
  push:
    branches: [main]        # triggers automatically on pushes to main
  pull_request:
    branches: [main]        # optional: triggers on PRs to main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Login to Docker Hub
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      # 3️⃣ Build & Push Docker image with tag ab-consul
      - name: Build and Push Docker image
        run: |
          docker build -t letting/ab-consul:latest .
          docker push letting/ab-consul:latest

      # 4️⃣ Setup SSH Agent
      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_DEPLOY_KEY }}

      # 5️⃣ Deploy via Docker Compose on server
      - name: Deploy via SSH and Compose
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} "
            cd ~/ab-consul &&
            docker-compose pull &&
            docker-compose up -d --force-recreate
          "
